services:
  testing-site:
    image: python:3.12-slim
    container_name: citizen-compass-testing-site
    working_dir: /site
    volumes:
      - ./:/site:ro
    ports:
      - "8080:8080"
    command: python -m http.server 8080
    restart: unless-stopped
