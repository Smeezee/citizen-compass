# Build truth

Compiled LIVE/PTU truth from game files. Call existing extractors; file results here (or pointer + index).
Do not scrape Erkul/SPViewer. StarBreaker is OUT.

Parent: [[../README|SC Brain]].
