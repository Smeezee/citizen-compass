# Memo

To:      Architecture (C1)
From:    Owner (Sleven)
Date:    2026-09-12
Status:  Answered
Subject: Next stage — finish the brain (B1→B2→B3), add an AAR loop, then efficiency installs

**Grok mapped the board with me. This is the order I want. Scope and sequence first; Build builds what you rule.**

---

## WHERE WE ARE

**Brain-two v0 is done and live:** `BOOT.md` regenerates, the beat runs, desk fetch records runs, boot prompts point at BOOT first, watcher source is in history (`183a239`, local, not pushed).

**Out of v0, still the project:** link index, router, repairer — already named in
`claude/RULING_brain-two-ships-the-digest-first-and-not-on-a-strangers-vault-2026-09-12.md` and
`NEXT.md` as B1 / B2 / B3 under the four-part design
(`claude/DESIGN_brain-two-what-it-is-in-four-parts-2026-09-12.md`).

**That is "completely done" for the brain.** Not a new vault. Not stranger self-heal.

---

## ORDER — FINISH THE BRAIN

### Stage B1 — Auditor + link index (unblocked; start here)
**DONE-WHEN** as already written in NEXT: one pass, two outputs — (a) findings report (dead citations that claim to be repo paths, satisfied-but-open entries, letters closed without ANSWERS, project-store docs with no file on disk); (b) derived link index Obsidian can graph. Flags only. Never fixes. Never gates deploy. Cost measured before it joins any sweep. Rebuild on timer / after sweep, not on every write.

**Also fold in Code's hanging proposal:** drop demoted `CURRENT-STATE` entries from `PRESENT_STATE_DOCS` in `named_thing_exists` so auditor wallpaper does not grow on a history file. Rule it with B1; do not leave it as an unanswered Build loop.

### Stage B2 — Router (blocked by B1)
A B1 gap becomes a letter through `inbox/` to the desk that can close it. Answers return through mail unchanged. **This is the loop.** No second research system.

### Stage B3 — Repairer (blocked by B2)
Fix **only from what we already hold**, propose a batch, apply on one Owner/Architecture word — never on a timer, never from the open web. Matches the design's FIX half. No wiki-self-heal autonomy.

### Stage ASK — later decision, not a Build scramble
Graphify **local** and/or Obsidian Omnisearch / Smart Second Brain (Ollama already on the machine). Install/decision, not a second brain product. After B1's index exists so the graph is not empty.

---

## ADD-ON — AFTER ACTION REPORT (ALL DESKS)

**I want an AAR skill in the stack** so Code, C1, Grok, Echo (via briefs), and CIC stop paying the same lesson twice.

**Adopt the shape of `aar-loop` (Army 4 questions → concrete checkable lesson → optional fix plan → `LESSONS.md`), not a launch postmortem doc that nobody loads.**

**Hard constraints (same as brain):**
1. **Proposal first.** Fix plans edit `CLAUDE.md` / skills / checklists only on approval — never silent.
2. **No open-web self-heal.** AAR does not fetch or rewrite the corpus from the internet.
3. **One `LESSONS.md` (or `claude/LESSONS.md`) pointed from `CLAUDE.md`.** Desks read it at boot or before similar work.
4. **Owner-ask gate stays.** An AAR that discovers "we should have asked Sleven" still runs the gate before any Owner chore.
5. **Echo gets a brief-shaped version** (issue checklist), not a private Claude skill she cannot load.

**Order:** Architecture write a one-page AAR procedure + skill stub; Build wires `LESSONS.md` pointer and a tiny append helper if needed; proposal before any auto-edit of rules.

**First AAR to run once the skill exists:** the last 48 hours (mail spine uncommitted, Owner-ask failures, CURRENT-STATE wallpaper, dual Design desks) — turn those into LESSONS, not another memo pile.

---

## STILL IN FLIGHT FROM PACKAGE BEST (DO NOT DROP)

Already ruled 2026-09-12 in `…package-best-five-owner-rulings…`:
1. Watcher source committed locally — **push only if you ask me; default is no push.**
2. Echo briefs / Grok tray holding — confirmed.
3. **No UEX data until written yes** — CIC draft the short ask for me to send; rentals from CIG rules until then.
4. **Erkul/CSG handoff for deep min-max only** — shopping stays ours. Design/Build when ready.
5. **Keep Testing gate until verification mark is on the page.**

---

## WHAT I WANT BACK

1. Confirm this sequence (or one-line corrections).
2. Scope B1 to Build (proposal already ordered — make it real).
3. Scope the AAR procedure (one page) before any skill install.
4. Reply through `inbox/`.

*Owner (Sleven), 2026-09-12. Drafted with Grok.*

---

ANSWERS:

# ARCHITECTURE DISPOSITION - 2026-09-12. CLOSED.

SEQUENCE CONFIRMED AS WRITTEN. No corrections. B1 to B2 to B3 to ASK, no parallel starts, FETCH stays CIC and Research and no second fetcher is built. B1 IS SCOPED AND ORDERED and Code is building it; the three questions you ruled are not being reopened. The `PRESENT_STATE_DOCS` item is folded into B1 rather than left as an unanswered Build loop, as you asked. THE AAR IS ALREADY REAL, NOT PENDING: the one-page procedure is `claude/PROCEDURE_the-after-action-report-2026-09-12.md`, the skill is `skills/aar-loop/SKILL.md`, the pointer is in CLAUDE.md and `claude/LESSONS.md` exists. Your five hard constraints are in the procedure as written. ONE CORRECTION TO YOUR TIMING, AND IT IS THE ONLY ONE: the first AAR does not run yet. `scripts/append_lesson.py` is not yet the sole appender to LESSONS.md and the file has no owner in OWNERS.md, so running the first AAR now writes a report into a file with two possible writers and an unowned path. That is rule 14 and we would redo the report. Days, not weeks.

*C1 (Claude-09), 2026-09-12.*
