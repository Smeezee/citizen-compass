# Memo

To:      Architecture
From:    Owner
Subject: CIC's sweep says none of the 253 rows is a package. That contradicts my own ruling, and one page read settles it.
Status:  Answered
Filed with CIC's report in full:
`claude/FINDING_two-claude-folders-and-a-sweep-that-asked-the-wrong-question-2026-09-12.md`.

## THE CONTRADICTION, AND NOBODY SHOULD BUILD UNTIL IT IS CLOSED

**CIC: none of the 253 rows is a package, and nothing any ship contains is missing from the
catalogue.**

**Me, three hours earlier: the Carrack Expedition "is just a extended package with those
ships packaged together."** Echo's three answers, the "Packages containing this ship"
section and the whole package design rest on that sentence.

**Our own data leans CIC's way.** The Carrack Expedition row is pledge-only with no store
link, and **its model file is byte-identical to the Carrack's, 876,628 bytes each.** That is
an edition, not a bundle.

**What settles it is one primary-source read: RSI's own Carrack Expedition page, and what it
lists as included.** Not an argument, not a vote. Route it to whoever can read the open web
and bring back the page's own words.

**Until then: no package section is designed, no card is removed, and Echo is told her three
answers rest on an unverified premise.** I would rather be told than obeyed into a hole, and
this is exactly that shape.

## TWO THINGS FROM CIC WORTH MORE THAN THE SWEEP

**1. Two `claude/` folders, and a write receipt that does not say which machine it wrote.**
The roles file went to the project store, four documents cited it, Build could not find it,
and Q63.8A sat blocked. **Your lag test could not have caught this** — waiting longer does
not produce a file that is on another machine. **A path in a memo names the machine, and the
writer reads it back off disk before citing it.** Third instance of this shape in a week.

**2. A sweep can reconcile perfectly and still ask the wrong question.** CIC's first pass was
wrong on six rows and reconciled clean, because the test required a full stop and RSI's
bullet lists have none — exactly where a ship says what it comes with. **Adopted: every sweep
carries one row whose answer is known from outside the sweep.** File that with the method,
not inside a result nobody reopens.

---

ANSWERS: Settled by primary source on 2026-09-12. Sleven opened RSI's Carrack Expedition
page himself and read the variant matrix. The Carrack Expedition matches the base Carrack
on every published field — manufacturer, focus, description word for word, production
state, 126 m / 74 m / 30 m, Large, 3,275,858 kg, 456 SCU, 140 m/s, 1,050 m/s, crew 6,
1x Surveyor (S2), 2x Internal Tank, 1x Kama (S3), 1x Internal Tank, 1x Exodus (S3). The
only differing field is the ship value. RSI's own words for the difference: a limited
edition livery. It is an EDITION and it folds, so Sleven's conclusion that the separate
card is unnecessary stands — on new grounds, not the ones he gave.

Research's sweep was correct and its headline was too narrow. None of our 253 rows is a
package because RSI keeps packages in a separate catalogue at a separate path, which was
never swept. WE CARRY NO PACKAGES. "Packages containing this ship" has no data behind it
and is not built; sweeping that catalogue is a separate job with its own reconciliation
and has not been ordered.

What the read produced instead is a THIRD RELATIONSHIP neither side had named: ten of our
ships COME WITH other vehicles, and all eight contained vehicles are already rows we
carry. Neither edition nor package — a link between two rows that both exist, and the
cheapest high-value item on the board. Separately, 53 rows carry RSI's own sentence "only
available as part of a PACKAGE", which is availability, moves, and is not a property of
the ship.

Method rule adopted, found by Sleven: a marketing render is never evidence of a variant
difference — RSI reuses art across SKUs. A ship's relationship to other ships is read from
the bullet list, never the description, the name or the picture.

Full ruling: claude/RULING_the-carrack-answered-from-rsi-itself-2026-09-12.md
