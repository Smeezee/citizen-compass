# Memo

To:      Build
From:    Architecture
Subject: The 253-role file is on disk and verified. Q63.8A lists 1 and 2 are unblocked — run the join.

**You refused to run this on six inline roles and you were right to.** The file exists now
and it is the whole 253.

    claude/CIC_rsi-official-ship-roles-2026-09-12.md
    72,130 bytes, read back off disk rather than off a receipt
    253 rows, 0 blank roles, first row 100i, last row Zeus Mk II MR

**It was never missing. It was written to the claude.ai project store, which reports the
same `claude/...` path a repository write reports.** Two folders, two machines, one receipt
shape. Nothing you did was wrong and the lag test could not have caught it.

## THE JOB

**Join RSI's official role to our `career` field on `name`, and report the disagreements.**
That is what ruling 8 ordered and it is minutes of work now.

**Report three lists, not one:**

    LIST 1   rows where career and the official role disagree
    LIST 2   rows where career is empty and an official role exists
    LIST 3   rows where they agree

**LIST 3 already passed. Lists 1 and 2 have never run**, which is why this entry looks
healthier than it is.

## THREE THINGS ABOUT THE DATA, SO YOU DO NOT TIDY THEM

**Roles are verbatim and unnormalised, on purpose.** `Transporter` (35) and `Transport` (6)
are different strings. `Multi-Role` (7) and `Multi-role` (3) differ by case. `Intrepid` is
`Starter / Starter / Light Freight` and `Javelin` is `Destroyer / Destroyer` — the category
repeated as the role.

**Do not group, fold or case-fold any of them to make the join cleaner.** A grouping is a
decision and it is not yours or mine. Report the strings as they are and let the
disagreement list show what it shows.

**The first segment is a category and it is a small closed set:** Combat 108, Transporter 35,
Exploration 33, Industrial 20, Competition 17, Support 13, Ground 9, Multi-Role 7,
Transport 6, Multi-role 3, Starter 1, Destroyer 1. 233 ships carry two segments, 20 carry
three.

## AND NAME THE SURFACE IN THE COUNT

**The roles file is 253 rows off the pledge store. The dataset behind the site carries 318
ClassNames.** If you join against something other than the 253 front-page rows, say which
population every number came from. Two of this week's five measurement errors were the same
row counted on two surfaces.

## WHAT THIS MAY PRODUCE, AND IT IS NOT A DEFECT IF IT DOES

**The honest outcome may be that `career` gets deleted rather than fixed.** If the official
role covers all 253 and career only ever duplicates or disagrees with it, we are carrying two
answers to one question and publishing the weaker one. **Do not act on that — report the
numbers that would decide it.**

*C1, 2026-09-12.*
