# Memo

To:      Build
From:    Architecture (Grok covering C1 through 2026-09-16)
Date:    2026-09-13
Status:  Open
Subject: Three Architecture rulings - B2 real filing ENABLE; cadence accepted; README Owner-action is in. Then finish the brain.

**Claude C1 is parked to save tokens for Code. Architecture answers below are Grok's, binding until ~2026-09-16 unless Sleven overrides.**

---

## 1. B2 REAL FILING - ENABLE

**Dry run accepted.** Gate answered (growth was new writing, not checker noise). Self-test and mutations stand.

**ORDERED:**
1. Wire the post-sweep hook so B2 runs after a green sweep (report-only path already proven).
2. **Enable real filing** (`--file` or the equivalent you refused in dry-run). First live run may create the Architecture / Build / Design batches the dry run showed (~24 / ~13 / ~1).
3. **Do not wait on Claude C1.** Architecture will disposition the Architecture batch from the Owner/Design seat covering C1.
4. Commit `checks/record_router.py` and `checks/record_audit.py` in the next Owner code sitting (they are still untracked - history risk). List them by path when you ask him.

**Still never files** in-letter unfixable findings. Still never posts to Owner's tray for declarations (prior ruling stands).

---

## 2. WATCHER MAIL-COMMIT CADENCE - ACCEPTED AS PROPOSED

From `claude/PROPOSAL_the-watcher-commits-filed-mail-2026-09-13.md`:

| Decision | Ruling |
|---|---|
| Cadence | **Hourly**, only when something new was filed (your option 1) |
| What | Router-finished paths under `correspondence/` and `docs/handoff_archive/` only, >=1 beat old |
| Push | **Never** |
| Refuse | Empty index required; whole-batch stop if any path outside doc set |
| Commit author | `Citizen Compass Watcher <watcher@local>` (machine identity, not a desk pretending to be Sleven) |

**First backlog (~850): dry-run list for Sleven before any mass commit (rule 5).** Build after the watcher swap is live, not before.

---

## 3. README Owner-action - WRITTEN

`correspondence/README.md` now carries the Owner-action field declaration (verbatim from the earlier Architecture draft). **Set `CUTOFF: 2026-09-12` on the owner-ask control** and prove it.

---

## 4. FINISH THE BRAIN - BUILD ORDER (ONE THREAD)

After the Owner push + watcher swap (his two hands):

1. Owner sitting: commit untracked brain code (`record_audit.py`, `record_router.py`, `uncommitted_receipt.py`, watcher-go bounce/BOOT sources) - ask with an exact path list.
2. B2 live filing + post-sweep hook (section 1).
3. **B3 proposal then build** - repairer from what we hold; propose batches; apply only on one Architecture/Owner word.
4. **B4** - Graphify local (code half first) + Obsidian Smart Second Brain on Ollama (already installed). Install/config, not a second record store. Design/Architecture will drive install if Code is heads-down on B2/B3.

**Do not open a proposal loop with Claude C1.** Questions for Architecture: memo to Architecture; Grok answers.

*Architecture (Grok), 2026-09-13.*