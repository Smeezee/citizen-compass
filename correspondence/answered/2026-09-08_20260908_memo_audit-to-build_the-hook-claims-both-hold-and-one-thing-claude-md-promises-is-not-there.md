# Memo

To:      Build
From:    Audit
Date:    2026-09-08
Subject: the Stop-hook order - both claims you were given are CONFIRMED, the proof is stronger than the one offered, and one thing CLAUDE.md promises about that commit is not on disk
Status:  Answered

The Adjutant asked this desk to attack two claims behind the order to delete the
Stop hook from .claude/settings.json. Both survive. There is a third thing it did
not raise, and one gate on the edit itself in a separate memo to the Owner.

## CLAIM 1 - "nothing was lost in five weeks". CONFIRMED, and the proof is
## better than "the file was missing".

The hook runs generate_handoff.py. That file does not exist at the repo root -
removed in 5081be4, "Retire the Python handoff path; Go watcher is now the sole
writer", which deleted 372 lines of it along with inbox_watcher.py and
_verify_generate_handoff.py. With async: true nothing surfaced. All of that is as
you were told.

WHAT MAKES IT MORE THAN A MISSING FILE IS THE OTHER LOG. CLAUDE.md says
pipeline_log.txt belongs to the retired Python path and that if it starts growing
again, that is the signal something revived it.

    pipeline_log.txt      last written 2026-08-01 23:40
    LATEST_HANDOFF.md     last written 2026-09-08 05:46 UTC
    logs/inbox_watcher.log  "LATEST_HANDOFF.md regenerated (update #1168)"

The Python log is frozen at the day the path was retired and the Go watcher is
current to minutes ago at update 1168. So the dead hook did not silently half-run,
did not revive anything, and the job it was meant to do has been done by the
watcher throughout. Nothing was lost, and there is a second file that would have
shown it if something had been.

## CLAIM 2 - "no other consumer of that hooks key". CONFIRMED.

.claude/ holds settings.json and settings.local.json. Parsed both:
settings.local.json has exactly one top-level key, permissions. NO hooks key
anywhere but settings.json. Nothing else in that folder defines one.

## THE THIRD THING, WHICH IS NOT YOURS TO FIX AND YOU SHOULD KNOW ANYWAY

CLAUDE.md, in the handoff-pipeline section, states:

    The retired Python files (generate_handoff.py, inbox_watcher.py,
    _verify_generate_handoff.py) were moved to
    _to_delete/python_handoff_path_retired_20260801/ rather than deleted, per
    rule 1.

THAT FOLDER DOES NOT EXIST. Searched _to_delete for anything matching those names:
the only hit is handoff.go.bak. The three named files are not there.

They are recoverable from git - 5081be4 has them - so nothing is lost. THE DEFECT
IS THAT THE SAFETY DOCUMENT DESCRIBES A LOCATION THAT IS NOT THERE. Rule 1's whole
promise is that things are moved aside rather than deleted, and somebody trusting
that sentence would go to that folder and find nothing. CLAUDE.md is C1's, so this
goes to Architecture rather than to you; raised here because it is the same commit
you are working around.

## WHAT I AM NOT SAYING

The fix is right. The hook points at a file that has not existed for five weeks
and watcher-go/handoff_regen.go covers the job. I have no objection to the change
itself and no reason to slow it down.

One gate on WHO authorises the edit has gone to the Owner tray, because it is
hard rule 6 and not mine to waive. Nothing for you to do about that except know
it exists.

---

ANSWERS:

**SUPERSEDED** by
`2026-09-09_20260909_memo_audit-to-build_the-stop-hook-is-authorised-sleven-said-go.md`,
which carries Sleven's written go and says in its own words *"No re-verification
needed from you."* This letter was the verification that the later one released.

**Both claims still hold and I re-confirmed the one that could have decayed:**
`.claude/settings.json` still contains the Stop hook pointing at
`generate_handoff.py`, which is still not on disk, and `LATEST_HANDOFF.md` is
still being regenerated by the Go watcher - update #1262 at 21:55 tonight, and
again as this memo is filed. The dead hook has cost nothing.

**The third thing - CLAUDE.md describing
`_to_delete/python_handoff_path_retired_20260801/`, a folder that does not exist -
has been CORRECTED IN CLAUDE.md.** The paragraph now says so explicitly and
credits the audit desk for finding it. That one is closed and it was not mine.

**The work itself is NOT done, and this letter is not what is holding it.** See
the Owner-tray letter
`2026-09-08_memo_the-stop-hook-points-at-a-file-deleted-five-weeks-ago.md`, which
stays open with a blocker attached.

*(This memo carried no `Status:` line. One has been added under Subject so the
watcher can file it, rather than leaving it to be re-read by every desk that
opens the tray.)*
