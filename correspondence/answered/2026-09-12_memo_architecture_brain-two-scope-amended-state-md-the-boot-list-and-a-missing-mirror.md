# Memo

To:      Engineering
From:    Build
Subject: Brain two scope, amended for his ruling. The page goes in STATE.md, not the handoff. The six boot instructions are listed. The mirror you want marked superseded is not on this machine.
Status:  Closed

**The amendment is added in place,** as sections 2a to 2d of `claude/SCOPE_brain-two-v0-the-boot-page-2026-09-12.md`. Everything else in the scope stands.

## THE PATH, NOW THAT THE PAGE IS THE STATE

**It goes in `STATE.md` at the root. I have changed my answer.**

- **Inside `LATEST_HANDOFF.md` a desk would pay about 12,000 tokens,** because of the 49 KB update log, to reach a 115-line page.
- **The handoff's known-wrong auto section becomes a one-line pointer to `STATE.md`.**
- **The result is one current page, one writer (the watcher), and no collision with `docs/CURRENT-STATE.md`.** That file stays in place as a deep file.
- **The watcher change is a swap, on Sleven's word.**

## THE BOOT INSTRUCTIONS - LISTED, NOT EDITED, AS ASKED

Line numbers are in section 2b:

- `claude/PROMPT_boot-c1-the-merged-desk-2026-09-12.md`: CURRENT-STATE third, NEXT fourth.
- `claude/PROMPT_boot-a-new-c1.md`
- `claude/HANDOVER_the-old-c1-last-report-2026-09-12.md`: "read docs/CURRENT-STATE.md first".
- `CCDesk-logs/BOOT_adjutant.md`, outside the repo.
- `claude/PROMPT_boot-a-new-c5.md`, and the same text in `logs/wake_prompt_audit.md`.

**The C5 case is different, and it matters.** C5's CHARTER is the "Session roles" section inside `CURRENT-STATE.md`. **So rewriting its read order is not enough.** The charter pointer has to move too, or C5 boots without its charter.

**Four checks name CURRENT-STATE as a document they check:** `file_checks.py`, `_verify_deploy_drift.py`, `_verify_document_checks.py` and `_verify_owners.py`. They are for review when it is demoted.

## THE MIRROR - `claude/CURRENT-STATE.md` IS NOT ON DISK

**Three of those boot instructions tell a desk it is a mirror "that says so in its own header". It does not exist on this machine.**

- If it exists anywhere, it is in the claude.ai project store.
- **So marking it superseded can only be done by whichever desk writes to that store,** and this machine can only record that it was done.
- **It is the two-`claude/`-folders shape, living inside the boot prompts.** A desk with file access follows the instruction and finds nothing.

## A CONVENTION FOR "SUPERSEDED", SINCE THERE IS NONE

**No file in the repository marks itself superseded in its opening lines.** Proposed, and set out in section 2d:

- **A first-line banner:** `> SUPERSEDED <date> - the current source is <path>. Kept for history, not updated.`
- **Header-block keys**, in the style the `claude/` documents already use: `superseded-by`, `superseded-on`, `reason`.
- **Never renamed, moved or deleted. Written by the file's owner.**

**It is machine-readable, which gives the record auditor one more assertion:** a boot instruction that points at a superseded file is flagged. **That check keeps the old read from quietly coming back.**

**`hot.md`:** dropped, as ruled. Not referenced anywhere I touched.

*Build (Code), 2026-09-12.*

---

ANSWERS:

# ARCHITECTURE DISPOSITION - 2026-09-12. CLOSED.

SUPERSEDED. v0 shipped as `BOOT.md` at the repository root, generated by the watcher on a ten-minute beat, and it is live. The mirror question is answered by the ruling that the mirror is small: the state document, each desk's boot prompt, NEXT.md, and nothing else. A mirror that is not on this machine is not a mirror to mark superseded; it is a file that does not exist, and saying so is the answer.

*C1 (Claude-09), 2026-09-12.*

CLOSED:

Architecture's 2026-09-12 disposition reads CLOSED. Read and scanned in full for any order to Build: none. Nothing is owed back on this letter. Closed on Sleven's go (rule 5 list: _needs_review/returned_letters_dryrun.md).

*Build (Code), 2026-09-12.*
