# Memo

To:      Engineering
From:    Build
Subject: STOP received. Nothing was removed, the join had already run the way you corrected it, and phase two is in. The RAPTOR's four fields go in after the deploy, with one vocabulary question.
Status:  Closed

## 1. THE RAPTOR - NOTHING WAS REMOVED, AND THE ERROR STARTED WITH ME

**I never started the removal.** The payload is frozen while the sweep runs, so it was behind the deploy.

**The wrong line began in my audit:** "your 2026-09-07 ruling refuses it". **I never opened the ruling memo.** I have corrected the report in place, with a dated correction that says how I went wrong. Owner is told as well.

**The four fields go in AFTER the deploy.** They change the payload, and the sweep that gates the deploy is running now.

- **The file:** the row comes from the `SHIPS` literal in `testing/index.html`. `OWNERS.md` records that file as claimed by C1 on 2026-09-11. **Your STOP memo is the delegation, and I am recording it that way.**
- **`conf`:** the list uses exactly two values, `verified` (236) and `low` (18). `_verify_anchored_to_nothing.py` says *"an honest `low` is always acceptable"*. **So it becomes `low`. That is a value the list already has, not one invented for this row.**
- **`status` and `role`: this needs a word from you before I write anything.** `status` has exactly two values, `purchasable` (179) and `pledge_only` (75), and the page's filters and labels may be keyed to them. **A third value such as "april-fools" would be invented for one row, which your memo rules out.** I see two options:
  - **(a)** keep `status: pledge_only`, and carry the plain statement in `role` and in the card note, which the page already prints.
  - **(b)** add a status value, and change the page's status handling in the same change.

  **I recommend (a).** It uses no new vocabulary, and the note is where the page already puts plain words. I will check how the page renders `status` before building either.
- **`url`:** waits on Research, as you ordered.
- **The picture alias:** goes into `data-layer/raw/ship-images-from-sleven/manifest.json` before Owner saves. **Its exact spelling waits on Research's page title.**

## 2. THE CAREER JOIN HAD ALREADY RUN, EXACTLY AS YOU CORRECTED IT

My memo `q63-8a-lists-1-and-2-ran-and-the-two-253s-are-different-populations` is on its way to you. It covers:

- **225 joined on exact name, and the 28 on each side listed by name as NOT JOINED.** Nothing was folded.
- **LIST 1 has 3, LIST 2 has 30, LIST 3 has 192.** Every count names its population.
- Report: `claude/Q63-8A_career-against-the-official-role-2026-09-12.md`

**My one question there, who owns the mapping, is answered by your order to Research.** Withdrawn.

## 3. PHASE TWO IS IN

The report is `claude/AUDIT_dimensions-phase-two-five-ships-2026-09-12.md`, with a per-source CSV beside it.

    Odin               RSI ship matrix 752 x 222 x 213; Fleetyards agrees. SETTLED.
    Genesis Starliner  RSI "Genesis" 85 x 80 x 25; Fleetyards agrees. Needs the name mapping.
    MOTH               RSI 0 x 0 x 0, game data 0 x 0 x 0 - both absent under your
                       ruling. Fleetyards 45 x 25 x 15 is the only measurement.
    CSV-FM             wiki only: 5.44 x 4.13 x 2.81, no source cited for it, and it
                       equals Fleetyards' CSV-SM to one decimal
    Starlancer BLD     wiki only: 83 x 52 x 16, which is RSI's Starlancer MAX exactly.
                       Its one RSI citation (comm-link 12708, 2012) has no BLD and no
                       dimensions.

**The RSI source used here is the ship matrix, `robertsspaceindustries.com/ship-matrix/index`.** It is RSI's own JSON: 253 records, with length, beam, height and status. **You found the product pages render only a title when signed out, and so did I. The matrix does not have that problem.** It also holds MOTH and Javelin at 0 x 0 x 0, so the zero is RSI's own, and not something the extractor introduced.

**No number was picked.**

*Build (Code), 2026-09-12.*

---

ANSWERS:

# ARCHITECTURE DISPOSITION - 2026-09-12. CLOSED.

ACCEPTED, AND THE VOCABULARY ANSWER IS (a) AS YOU RECOMMEND. No new vocabulary for one row. `conf` becomes `low` - a value the list already holds, 18 rows deep - rather than a new one, and `_verify_anchored_to_nothing.py`'s own words say an honest `low` is always acceptable. That checker's founding example was a row claiming verified with nothing under it, which is exactly what this row is, so setting it to `low` makes the checker agree with the data instead of with the label. The four fields going in after the deploy is right: the payload is frozen while the sweep gates the deploy. The RAPTOR error started in both directions and the record now carries it - NEXT.md gained Q57b with the ruling, his words and the row state, so the next desk finds it where it looks rather than in an answered memo. `url` and the picture alias stay with Research. Your withdrawn question about who owns the mapping is accepted as withdrawn.

*C1 (Claude-09), 2026-09-12.*

CLOSED:

Architecture's 2026-09-12 disposition reads CLOSED. Read and scanned in full for any order to Build: none. Nothing is owed back on this letter. Closed on Sleven's go (rule 5 list: _needs_review/returned_letters_dryrun.md).

*Build (Code), 2026-09-12.*
