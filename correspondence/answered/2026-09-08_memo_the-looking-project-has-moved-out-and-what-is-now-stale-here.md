To:      Engineering
From:    Sleven
Date:    2026-09-08
Subject: the Looking Project has moved out, and what that leaves stale here
Status:  Answered

The Looking Project is its own project now and it has its own folder on my
machine. It is not part of this one. This project is a client of it — a thing it
can be pointed at — and that is the whole relationship.

**Nothing inside it names this project anywhere.** Not a table name, not a path,
not a comment, not an example, not a default. Somebody reading it cold cannot
tell what it was first pointed at. The one place this project appears is a
clients folder, described as the first host and not named.

It gets its own repository later, and that project will get access to this one to
keep building it. Until then it is built from here, because this is where the
hands are.

## WHAT MOVED

The reasoning. The boundary ruling, the name, the eight conditions, the method,
the measured grader and the open lens question are now maintained over there and
only there.

## WHAT DID NOT MOVE, ON PURPOSE

**The memos.** They are this project's mail — letters between this project's
desks, filed in this project's trays. Pulling them out would put holes in the
record for nothing. They stay exactly where they are.

## WHAT IS NOW STALE ON THIS SIDE, AND IS YOURS

Two files here still state Looking Project positions. They are now copies, and
copies drift — this project has already paid for that three times with its state
documents.

**`design/ANGLES.md`** — the method now lives in the Looking Project. What is
genuinely this project's is the checklist for our own pages. Keep that, and
replace the doctrine half with a pointer.

**`docs/ARCHITECTURE_DECISIONS.md` section 4** — leave the locked text alone. It
is correct and it is this project's decision about where its own findings go. Add
one line noting the machine it refers to is a separate project now.

Anywhere else that states a Looking Project decision, do the same: point, do not
restate.

## WHAT DOES NOT CHANGE

Nothing is authorised to be built. That has not moved either.

Section 4 stands word for word. Its clarification — that the destination is
supplied by this project and not known by the machine — turned out to be the same
principle the separation runs on, and it was right before there was a folder.

## WHAT TO SEND BACK

One line saying the two files are pointed rather than restated. Not a design.

---

ANSWERS:

**Architecture, 2026-09-08 — one file pointed, one file left alone, and the second half of the order was carried out on 2026-09-09.** `docs/ARCHITECTURE_DECISIONS.md` Section 4 now carries a NOTED paragraph saying the machine it refers to is a separate project and that its reasoning is maintained there and only there; the locked text is untouched word for word. **`design/ANGLES.md` was not changed, because it stated no Looking Project position at all** — a search of every current document in the repository, excluding mail and archived history, returned only the one file. Two things with similar names had been joined: *sixty angles* is the examination method, *ten ways to look* is Design's reader inventory. **You confirmed that reading and then moved ANGLES for a different reason** — it is your method, it predates the site, and more than one project uses it — so it now lives at `CCDesk-logs/ANGLES.md` with a pointer here plus the questions specific to this project.
