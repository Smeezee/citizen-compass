<!-- OCR transcription of 20260728_195237_test2.png, 2026-07-28 19:52:40. Original image kept at: 20260728_195237_test2.png (see image archive folder). -->

Oo Qeé

Resolving Inbox_watcher py file structure Issue

extraction,and shutil.move() (calledfrom route_simple onthe
zip itself, right after) can collide with that. This is a known
Windows-specific flakiness with shutil operations happening in
quick succession on freshly-extracted files — Linux doesn't have
this problem, which is exactly why my sandbox testing didn't catch
it.

Net effect: your two files' .py content did get correctly extracted
and saved, but bigboys.zip itselfisstillsittingin inbox\ ,not yet
archived. It'll likely get retried and succeed on its own, or you can
just leave it since nothing was lost — but let's not lose track of it.
Let's get back on track for the actual test. With the watcher still
running:

1. First,clean up: delete bigboys.zip from inbox\ ifit'sstill
sitting there (check with dir inbox ), since we don't need to
keep retrying it.

2. Delete the two accidental duplicate files it created
(image_handling _26260728192926. py,

inbox_watcher__20260728192926. py ) — those aren't needed,
they're just stale copies from that zip.

3. Now drop an actual screenshot with text into inbox\ — that's
the real test we're after.

‘Want to do that now? Paste back what the PowerShell log shows this
time.

Q%eoec

*

this isa screenshot

+ Sonnet5 Medium» @

Claude is Al and can make mistakes. Please double-check responses.

Image handling PY

10
a
2
13
14
15
16
v7
18
19
20
a

23
24
25

26
27

28
29
30
aA
32
33
34
35
36
37
38
39
40

Citizen Compass — Image handling for inbox_watcher.py

Drop-in module: import into inbox watcher.py and call handle_image_file()
from the main dispatch logic whenever a .png/.jpg/.jpeg/.bmp/.webp shows up
in inbox/.

WHAT IT DOES NOW:
Screenshot of text (chat log, terminal output, error message, notes) ->
OCR'd with Tesseract -> saved as a .md file -> handed back into the SAME
inbox/ folder so it flows through the existing .md classification logic
in inbox_watcher.py unchanged (handoff doc / update doc / plain doc).
The original image is archived, never deleted.

WHAT IT STUBS FOR LATER:
Screenshot/photo with little or no readable text (a ship render, a UI
mockup, anything that's “a picture of a thing” rather than "a picture of
words") -> routed to needs _revien/images/ instead of the generic
_needs_review/, and see analyze _image_content() below for where a local
Vision model (e.g. Ollama + Ilava/quen2-vl) gets wired in later.

ROUTING RULE:
OCR word count >= TEXT WORD THRESHOLD -> treated as a text screenshot
OCR word count < TEXT WORD THRESHOLD -> treated as a content image,
stubbed for future vision analy
sis

Requires: pytesseract (pip install pytesseract), Pillow (pip install pillo

”),

and the Tesseract OCR engine itself installed and on PATH.
Windows: https: //github.com/UB-Mannhein/tesseract/wiki (installer .exe)
after installing, either add its folder to PATH, or set
pytesseract..pytesseract.tesseract_cmd to the full exe path (see
TESSERACT_CMD_OVERRIDE below) if it's not auto-detected.

from pathlib import Path
from datetime import datetime

8 Activate Windows
import pytesseract Go to Settings to activate Windows.
from PIL import Image
