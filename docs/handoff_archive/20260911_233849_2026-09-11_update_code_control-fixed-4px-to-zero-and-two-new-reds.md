# Update — the control is fixed and proven, the 4px went to zero, and two new reds hold the deploy

**2026-09-11 23:40 CDT / 2026-09-12 04:40 UTC.**

## FIRST, WHAT I GOT WRONG TONIGHT

**I told Sleven all evening that my letter asking Architecture to rule the red
control was unanswered. It had been answered at 19:57.** I was reading my tray
by the newest three or four files, and returned answers arrive in batches, so
older ones fell below the cut. **Four items missed:** that answer, the answer on
the 4px count, Owner's usage-logging order, and a returned memo of my own.
**Read the whole tray by status now, not by the top of an mtime listing.**

## DONE

**The correspondence control — Architecture's order, delegation recorded.**
Closed/Done now need `ANSWERS:` or `CLOSED:` (20+ characters); Answered still
needs `ANSWERS:`; docstring matches the code. **Proven by mutation both ways,
self-test 19 of 19.** The `CLOSED:` marker added to the RAPTOR letter, Owner's
text verified byte-identical — directly, not by `inbox/` re-drop, because CIC's
duplicate bug would have left the unmarked original in place.

**Q58 defect one — to zero, by the walk, as ordered.** `.pr` margin 6→3px, on top
of the 4px already in. **+2 reached zero at the walk's 0.5px threshold with 0.3px
sub-pixel overshoot left; +3 is the first with none at all**, at 1510/900/560/390,
every card still 152px — so the threshold did not decide it. **Why 4 was short,
recorded in the CSS:** the arithmetic counted line-height only; the last line box
of an 11px italic note overshoots by descender and half-leading, ~2.3px.
**Defect two is Architecture's** — the caveat leads, at both origins, separate
change after this payload.

**Owner's usage-logging order — already done, answered with evidence.** The
launcher writes input, output, cache created, cache read, num_turns, cost and
session; the log holds a real record from the 2026-09-10 probe. Nothing changed.
Step 2, the three wakes, not started.

## HELD

**T-008 and the +3 are in the payload, drift 16/16, nothing deployed.** The real
trays are red on **two more Architecture letters closed by Owner at 20:11 in the
same unmarked form** — after the 19:14 sweep, so not caused by the amendment.
**Not marking them: the order named one letter.** Asked Architecture for one
word, and flagged it as a class — every letter Owner closes that way will block a
deploy.

*Code, 2026-09-11 23:40 CDT.*
