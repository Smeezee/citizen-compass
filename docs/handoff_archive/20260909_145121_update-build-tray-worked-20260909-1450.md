# Update — the build tray is picked up, eight letters, worked oldest-value-first

**2026-09-09 14:50 CDT.** Sleven said "go" against the plan filed at the top of
this session: work the build tray to empty.

**Rule 24 re-read first.** Mail has arrived since the last read — twenty files
across six trays overnight on 2026-09-08, one of them for this desk:
`four-items-for-build-a-material-a-defect-two-checks-and-three-eyes.md`. It is
counted below. The sweep receipt is unchanged: 2026-09-07 22:42, 124 passed, 0
failed, 0 not-run, 3 `_deployed` controls skipped.

## WHAT IS IN THE TRAY — EIGHT LETTERS

    1  two comments say "audio is out" when they mean the collector must not
       make a NOISE                                        wording only, 2 lines
    2  build the six document checks in checks/file_checks.py   the substantial one
    3  four items for build — hull material, normals defect,
       two checker additions, register three eyes          arrived since last read
    4  the hook claims both hold and one thing CLAUDE.md promises is not there
    5  the stop hook points at a file deleted five weeks ago
    6  owner ruling — never read their code
    7  the overlay question moved and somebody already built ours
    8  one more assertion for the correspondence control — undelivered post

Items 3(a) and 8 are the same subject — the bounce folder — arriving twice from
two desks. They get built once.

## ORDER

Wording fix first because it is two lines and closes a letter. Then the six
document checks, then the correspondence-control additions, then the three eyes,
then the hook items. **Each one gets its own update as it closes** — rule 13,
one unit of work per file, not one file covering the tray.

**Nothing is committed.** The working tree already carries ~60 modified files on
top of `c8ab1d0` and no go-ahead has been given for any of it.
