# Update - the exit-2 dry run is done. 12 files, 18 sites. Nothing edited.

Rule 5 pass, report only. `checks/_diag_notperformed_exit2.py` writes nothing
and can be re-run by anyone. This is the list Sleven sees before a byte moves.

## The one fact that decides whether this is safe at all

I checked it rather than assuming it:

    run_all_controls.py:488   return 1 if (failed or not_run) else 0
    sweep_gate.py:237         bad = failed + not_run

**NOT RUN blocks the sweep and blocks the deploy gate exactly as FAILED does.**
So moving a site from exit 1 to exit 2 does not weaken a single gate. It changes
only what the control says about itself - "I looked and found a defect" becomes
"I could not look" - and both stay red. If NOT RUN had been non-blocking this
whole change would have been a silent-success factory and I would have stopped.

## The 12 files, 18 sites

    _verify_child_markers.py           175   sys.exit("...NOT PERFORMED.")  -> 1
    _verify_deploy_drift.py           1047   return 1   PAGES unreadable
    _verify_deployed_links.mjs         498   exit 1     top-level catch, sweep threw
    _verify_disclosure.mjs             356   exit 1     its own notPerformed list
    _verify_find_build_step.py         133   return 1   no priced row to mutate
    _verify_find_deployed.mjs           58   exit 1     die()
    _verify_imported_models.mjs         86   exit 1     no manifest
    _verify_imported_models.mjs         95   exit 1     no ships selected
    _verify_imported_models.mjs        144   exit 1     viewer reports no DRACO
    _verify_inspector_nose.mjs          87   exit 3     mine, and 3 means nothing
    _verify_marker_positions.mjs        71   exit 1     no overlay
    _verify_marker_positions.mjs       102   exit 1     overlay has no Gladius
    _verify_marker_positions.mjs       118   exit 1     refuses to clobber an attic
    _verify_model_scale.mjs             77   exit 1     no scale_fix_report
    _verify_model_scale.mjs            102   exit 1     no known-bad files
    _verify_rule16_labels.py           216   return 1   no baseline
    _verify_version_single_source.py   116   return 1   set_version.py missing
    _verify_version_single_source.py   135   return 1   the build failed

`_verify_inspector_nose.mjs` is mine and its `notPerformed()` exits **3**. The
runner maps only 2. That one has been reporting a plain failure since I wrote it.

## Two the survey flagged and I rejected after reading them

Named, not quietly filtered, because a survey that silently drops things cannot
be checked by anyone but its author.

    _verify_hull_solid.mjs:428      the SUCCESS path - disclaims one sub-measure
                                    and exits 0. Correct exactly as written.
    _verify_deployed_links.mjs:428  the dead-canary branch calls fail() and
                                    carries on; not an exit site. Whether a dead
                                    canary should be NOT PERFORMED rather than a
                                    counted failure is a real question about what
                                    that control means - a judgement, not a
                                    relabel. Left alone, raised here.

## Ownership

**None of the 12 is C1's.** Checked against `OWNERS.md` by name, not against the
list I had typed from memory. C1's `_verify_hull_is_solid.mjs` is a different
file from `_verify_hull_solid.mjs`, which I excluded anyway.

## What I cannot prove today, and will not pretend to

The current sweep is 120 passed, 0 failed, 0 NOT RUN. **Not one of these 18 sites
is firing right now**, so a green sweep after the change would prove nothing at
all - it would be the same green either way. Rule 12: a gate whose failure path
has never executed is untested.

So each site has to be made to fire on purpose - take the manifest away, point it
at a missing baseline - and the exit code read from the outside. That is 18 small
provocations, and it is the actual work here; the edit itself is one character in
most of these files.

## Stopped

Rule 5 says report-only first, print what would change, and stop until Sleven has
seen the list. He has not seen it yet. Nothing is edited.
