# UPDATE — Cowork session handoff, overnight verification running (2026-07-31)

Written by Claude (Cowork) directly to `inbox/`, not by Claude Code. Covers work done in the Cowork session tonight so `LATEST_HANDOFF.md` has it alongside Claude Code's own updates.

## Running right now

**Overnight job:** exhaustive read-only verification of the scunpacked-data snapshot via `scripts/external_sources/verify_snapshot.py`.

- Target: `data-layer/external-sources/scunpacked-data/snapshots/20260731T041451Z` (handles the `.partial` variant too)
- Output: `data-layer/external-source-verification/20260731T041451Z/`
- Produces `verification-report.json`, `SHA256SUMS.txt`, `duplicates.json`, `journal.jsonl`, `verify.log`
- Expected 30 min – a few hours. Resumable — re-run the same command if interrupted.
- Closes the gap where only 40 of ~28,959 JSON files were ever parsed (0.14%), the malware scan was never run, the domain scan was skipped, and no per-file hashes existed.
- **Do not modify this script.** It was written and tested in Cowork, then pushed. Safety verified rather than assumed: opens every source file `rb` only, no delete/rename/network/subprocess calls anywhere, refuses `--out` inside the snapshot, and a test run was proven byte-identical before and after by checksumming the whole tree.

**First thing to check:** `coverage.inspection_complete` in `verification-report.json`. If false, the run does NOT constitute full coverage and must not be reported as if it does.

**Deferred decision, deliberately:** if the scan comes back clean, do NOT rename the snapshot back to `.partial`. Record the scan and re-status it as properly complete. Quarantine only if something is actually found. This is why FIX 1 was held back from tonight's batch.

## Completed in Cowork this session

- **Full project audit** against the live repo — 20 severity-ranked findings, plus four claims that did not survive re-verification and are reported as refuted. Notably: the audit's original #1 finding (that `run_e2e_test.py` drops a database named from `DATABASE_URL`) was WRONG — `DB_NAME` is a fixed prefix plus a random suffix, never derived from it. Corrected.
- **`CLAUDE.md` rewritten** — 11 hard destructive-action rules at the top, plus four standing rules that previously existed only in chat history (no execution against an unsaved Blender scene, Fan Kit/legal text off-limits, no commit/push without go-ahead, no working around a blocked fetch). Stale content fixed: it still described `inbox_watcher.py` as Python when it has been a Go binary for days.
- **`run_e2e_test.py` guarded** — refuses hosted/managed or non-local database servers and an unset `DATABASE_URL` (which silently fell back to `RAILWAY_DATABASE_URL`, i.e. production). Re-checks the throwaway name immediately before each destructive call. Guard logic unit-tested across 7 cases.
- **Backup script v2** — fixed the PowerShell 5.1 bug where native stderr becomes a terminating error, which killed v1 at step 1 on a *passing* `git bundle verify`.
- **Canonical-source decision made: Option B, field-level source authority.** Different fields have different ground truths, so no single source can be canonical. Five tiers, six conflict rules. Key constraint: sources 1 and 3 share upstream lineage, so their agreement is not corroboration and any voting scheme would produce false confidence.

## Confirmed by the completed backup

`C:\cc-backup\20260730-233853`, 502.3 MB, mirrored to `E:\` with all 598 files hash-verified. `citizen_compass_hardpoints.py` was captured from the live Blender install — it existed nowhere in the repo and would have been lost entirely.

The restore test's "232 vs expected 254" warning was a wrong hardcoded expectation, not a bad dump. But it surfaced something real: **the live site (254 ships, from static `preview.html`) and Postgres (232) disagree by 22 ships.** Reconcile before Stage 2.

## Still queued — the three small fixes, NOT yet run

1. **FIX 1** — rename the snapshot to `.partial`. Now conditional on the overnight scan result, see above.
2. **FIX 2** — delete `app/models.py:197`. It declares `Index("ix_components_component_type_id", ...)` in `__table_args__` while lines 200-202 declare the same column with `index=True`, auto-generating an identically named index. Confirmed empirically in SQLAlchemy 2.0.51: `Base.metadata.create_all()` fails.
3. **FIX 3** — make `integrity_scan.py` and `finalize_star_citizen_wiki.py` exit non-zero on findings. Both currently always return 0, so wiring them with `&&` fails open and promotes snapshots that failed their own checks.

## Still open

- Offsite backup copy — both current copies are in the same trailer. Recommendation: Backblaze B2 with compliance-mode Object Lock. Needs Sleven's sign-in and card.
- Copy the backup folder to the laptop being taken.
- Site vs DB 22-ship gap.
- Departure for Minnesota **Aug 3**.

## Standing

Nothing was committed or pushed this session. All edited files are tracked in git and revertible.
