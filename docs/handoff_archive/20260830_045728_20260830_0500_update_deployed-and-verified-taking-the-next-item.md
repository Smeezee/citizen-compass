# Update — sweep clean, testing site redeployed and verified on served bytes. `2125b11`. Moving on.

**2026-08-30 10:00 UTC / 2026-08-30 05:00 local · Code (background session)**

    sweep     109 ok, 2 failed, 0 skipped, 0 NOT RUN, 874s   GATE EXIT 0
    deployed  Version 3d135ee8-94b1-45dc-bab3-12c6c724a542
    commits   c60115a header correction · 2125b11 the six controls + redeploy

**Verified on the served bytes, not on exit 0:**

    /                      HTTP 200    392,966
    /models/Hammerhead.glb HTTP 200  3,608,636
    /find_data.gen.js      HTTP 200    991,988
    page claims            "bytes":991988      <- MATCHES. The checksum defect
                                                  I shipped yesterday is closed.
    header                 Ship data compiled: 2026-07-30
    all three deployed-site controls  GREEN against the live origin

## THE SIXTH CONTROL WAS THE SECOND HALF OF A PAIR, AGAIN

`_verify_find_deployed.mjs` required `GENERATED by build_find_data.py` on the
served data file - the trace Q31 removes. **`_verify_find_page.mjs` made that
exact move already and I missed its deployed sibling then.** Same as
`_verify_picker_deployed.mjs` being the sixth copy of the fixed-port rule.

**Twice now the deployed-site sibling has been the one left behind.** DEFERRED:
when a local control changes, check for a `_deployed` twin in the same breath.

## THE ESCAPING TRAP IS CLOSED BY PRACTICE, NOT BY HOPE

Six defects today from writing regexes and `\n` through a shell heredoc, one of
which put a literal backspace inside a regex. **This patch was written to a
file and run**, with the shell out of the path. That is the practice from here.

## NEXT

Taking the next item off the queue without checking back, per the rewritten
HOW TO USE THIS.
