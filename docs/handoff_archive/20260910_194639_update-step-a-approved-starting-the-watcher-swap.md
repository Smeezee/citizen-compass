    state        WORKING
    blocked_on
    clears_by

Filed at 19:47 CDT / 00:47 UTC — `date` run at 19:44:58, this typed straight
after it.

# STEP A IS APPROVED. STARTING THE WATCHER SWAP NOW.

## WHAT ARRIVED

    19:43  Owner   STEP A APPROVED - swap the watcher, then stop. Step B is a
                   separate authorisation and I do not start it.
    19:44  Owner -> Architecture   the intake tray, the rotations
                   recommendation, the Perplexity export design. NOT MINE, read
                   because rule 24 says read the mail, not read my own post.
    19:34  claude/STATUS_desk-automation-completion-and-proposed-order-2026-09-11.md
                   section 10 carries eight questions. **None blocks step A**
                   and they are answered when the swap is reported.

## THE ORDER, AS I READ IT

    1  preserve the running binary under a dated rollback name FIRST
    2  install the replacement
    3  verify FOUR conditions on the LIVE tree, synthetic letters only
    4  close out every test letter and name it in the report
    5  report the evidence and the rollback location, AND STOP

**I do not begin step B.** Containment comes back to him as its own
authorisation request, written by me, after this is reported.

**Not authorised by this letter and not being done:** the master switch stays
OFF and he activates it, nobody else, after the fifteen-point acceptance test ·
the watcher is NOT connected to the launcher · no database work · no deployment ·
no unrestricted execution · the Looking Project is excluded entirely.

## THE CONDITION I EXPECT TO BE HARDEST, SAID BEFORE I ATTEMPT IT

**Condition 2: show the open copy surviving a routing FAILURE, not only a
success.**

A success is easy and proves half of it. **The guard is `if e == nil && answered`
— the supersede only runs when the file actually landed** — and demonstrating
that means making a real `routeTo` failure happen on the live mail system.

**My plan is to occupy the destination filename with a file held open by another
process.** `routeTo` tries to move an existing destination aside before writing;
a locked file cannot be renamed, so it returns its error and the supersede never
runs. That is a genuine induced failure and it is reversible — no permission
change, no filesystem fault, nothing deleted.

**If that does not actually produce a failure I will report that it did not**,
and say plainly which half of condition 2 is proven and which is not. A test
where the failure never happened is no test, and reporting one as passed would
be worse than reporting it as not performed.

## AND A CORRECTION HE OWED ME THAT I AM CLOSING

His letter telling me to file the ACL audit is superseded — **it was already
filed at 19:24:50** before that letter arrived. Closing it as superseded, not as
answered.

## THE TRAY RECONCILIATION AND THE ACL SCAN

**Both noted, neither started, neither delays the swap.** Roughly fifteen of the
build tray's thirty-two letters are spent; closing them produces an auditable
record of what was closed and why, and **that record is the deliverable, not the
tidy tray.** The descendant ACL scan goes last in the queue.

Nothing committed. Nothing woken. The switch file still does not exist.
