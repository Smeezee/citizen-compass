# Update — tray state after the clear-out

Ten memos down to five. Six stale ones from 08-31 and 09-05 answered with what
actually happened rather than "superseded" — a memo closed with no substance is
the filing defect `_verify_correspondence.py` exists to catch.

**Closed:** the eight reds (all gone — today's sweep is 122 green, 0 failed, 0
NOT RUN), the batch build-and-deploy (done and verified against served bytes),
both corrections, the dealer-price finding, and the overnight marks.

**Still open and genuinely live:** the in-game data gap memo that arrived today,
the RAPTOR STOP/resolution pair, and the ruling on the five.

**Written today and green:** `_verify_one_fleet_two_files.py`,
`_verify_front_page_prices.py`, `_verify_anchored_to_nothing.py`,
`scripts/push_main.ps1`. Label gate 126 labelled / 0 unlabelled;
`_verify_owners.py` passes.
