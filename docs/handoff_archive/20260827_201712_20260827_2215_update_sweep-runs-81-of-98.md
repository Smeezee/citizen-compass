# Update — The sweep runs again: 81 ok, 14 failed, 3 skipped, 0 NOT RUN, in 613s.

**2026-08-27 22:15 · Code (background session)** — follow-on to the 22:00 note.

    81 ok, 14 failed, 3 skipped, 0 NOT RUN, in 613s

**98 controls ran.** Before the two encoding fixes it stopped at 14.

## The 14 failures were already failing. They were just invisible

Nothing here was broken by the fix — the fix is what made them reportable. A
suite that crashes at control 14 of 98 hides everything after control 14.

    _verify_broken_checker_end_to_end.py  including the 6 genuinely-missing models
    _verify_child_markers.py              a hull changed with no nested eligible port to inherit from
    _verify_deploy_drift.py               FAILED
    _verify_deploy_guards.py              FAILED
    _verify_dim.mjs                       1 of 43
    _verify_g3_matcher_delta.py           VERIFY FAILED (1)
    _verify_hardpoint_alignment.py        VERIFY FAILED (1)
    _verify_hardpoint_join.py             VERIFY FAILED (1)
    _verify_model_resolution.py           FAILED
    _verify_placer_candidates.py          a previously placed hull is no longer byte-identical
    _verify_rule16_labels.py              _verify_placement_gate.py has no RULE16 label
    _verify_ship_gaps.py                  Eclipse has markers where the control expects none
    _verify_ship_page.mjs                 N11: one control restores all 21 editable ports to stock
    _verify_stage_panel.mjs               1 of 52

**Several of these are today's own work showing up honestly**, not regressions:
`_verify_ship_gaps` expects the Eclipse to have no markers and it now has ten;
`_verify_placer_candidates` expects previously placed hulls to be byte-identical
and one is not. Those controls encode yesterday's fleet. **A control that
disagrees with a deliberate change is doing its job and needs re-baselining by
whoever made the change** — mostly C1's hardpoint work, some of it my imports.

**One is mine and is correct as-is:** `_verify_rule16_labels.py` fails because
`_verify_placement_gate.py` carries no label. That is the ratchet working, and
the fix is one line from that file's author.

I have not touched any of the 14. Re-baselining someone else's control to make a
sweep green is how a suite stops meaning anything.

## Three SKIPs, each named with a reason

    _verify_deployed_links.mjs    ~450 network requests against the deployed origin
    _verify_find_deployed.mjs     a statement about the live site, not this tree
    _verify_picker_deployed.mjs   clicks all 1,200 hull markers over the wire

Opt-in by design, and "skipped" is never printed as "passed". I ran
`_verify_deployed_links.mjs` separately after each deploy today anyway.

## What I did NOT do

**I did not write a runner.** `checks/run_all_controls.py` already existed and
is well built — discovery instead of a typed list, fail-closed NOT RUN
accounting, a `--self-test` that demands a non-zero exit from every inverted
control, and a 900s timeout recorded as NOT RUN. A second one would have been
the duplicate-writer defect.

The only changes are the two rule 15 fixes, in the author's own style, with the
crash that motivated each written at the site.
