# Update — AUTOMATION FIRST received; the existing queue steps back

Filed at the time in this file's archive name. Rule 13, on arrival, before
starting.

## WHAT ARRIVED

`correspondence/open/build/2026-09-10_memo_automation-first-prove-one-desk-can-be-woken-then-build-the-brakes.md`,
from the Owner. **It displaces the list.** Read on arrival with the rest of the
tray.

## THE STEP I AM TAKING, AND ONLY THIS ONE

**Start one desk headless, have it read ONE letter, write ONE reply, and exit.**
Not a loop, not a schedule, not two desks. On a letter that does not matter, and
watched.

Reporting with it: the token counts from `--output-format json`, the dollar figure
**marked as the client-side estimate Anthropic describes it as**, and **which
allowance it actually drew from** — that last one is the claim the whole design
rests on and the memo is right that it has been read and never observed.

**Not touching, because the memo says not to:** Temporal, the Agent SDK, the
brakes (Architecture specifies those and I build what it specifies), and
everything in section 5.

## THREE THINGS FROM SECTION 5 THAT ARE ALREADY CLOSED

Filed tonight, before this memo arrived, so the list may not have caught up:

    the six old letters       DONE - 4 answered and cleared, 2 stay open, 0 binned
    the authorised small import   DONE - it had already executed on 2026-09-07;
                                  wiki_ship_fields.json, usd 76 and url 10
    the red API-key case      DONE - the state is NOT impossible. Test-Path was
                              the defect; the case is kept and the guard fixed.
                              30 passed, 0 failed, both new checks seen to fail
                              on known-bad input.

**And the stamp at `build_deploy.py:842` is measured but held** — `tzdata` is a
real dependency, `zoneinfo` resolves zero timezones on either interpreter here,
and the deploy calls the SYSTEM python. Detail in the update filed just before
this one.

**B2 and `family_id` staying blocked matches what I found:** it was never started,
and the RAPTOR exception went live again when Sleven kept the row as a joke card.
Architecture's ruling, not a guess.

## AND ON THE STOP HOOK

Noted: *"Ask for it when you next need it and I will answer the prompt."* I will
ask at the point of doing it rather than sitting on it.

Nothing committed.
