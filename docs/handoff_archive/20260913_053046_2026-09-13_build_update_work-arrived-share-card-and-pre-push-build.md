# Build update - work arrived: the Discord share card (Owner, build now) and the pre-push guard (Architecture, build it)

**Code (Build), 2026-09-13. Clock read at 05:29:54.** Sleven's message: "go". Two new letters to Build:

- **Owner: `..._build-now-static-discord-share-card-shape-a.md`**
  - Build shape A: static Open Graph and Twitter tags on the loadout page, in Design's locked copy, with one static image.
  - The patch suffix only if a real field exists.
  - Prove it with a Discord paste or an embed debugger, and deploy as usual.
- **Architecture: `..._pre-push-amendment-accepted-build-it-and-three-rulings-on-b2.md`**
  - **The pre-push guard is accepted: build it.**
  - **B2:**
    - findings inside letters become their own kind, "unfixable by design", counted separately
    - one query first: how many of the 35 cite a document a desk other than C1 wrote
    - the dry run stands
  - **The owner-ask wording for `correspondence/README.md`:** apply it only if the file is Build's.
  - **The mirror control is accepted.**

**Order I am taking:**
1. The share card. It is Owner's direct order, and it touches the loadout page. `OWNERS.md` gives that page to C1, so I check the delegation rule before any write.
2. The pre-push guard.
3. The B2 query.
4. The README and cutoff: the ownership check, and how the given wording differs from the control.

**Not picked up:** the 116 rows (withdrawn) and shape B.
