# Update — Code is taking Q54, the swap

**Received:** "go" from Sleven, 2026-09-11 01:25 CDT / 06:25 UTC. Q53 closed at
01:23, so the queue's next item is **Q54 — `/next` becomes the front door**,
which was BLOCKED-BY Q53 and is now unblocked.

**Mail read first (rule 24):** nothing new since my last reply — newest in
`correspondence/open/build/` is still last night's 22:19 switch memo, already
answered. `checks/.last_sweep.json` unchanged, 128 passed.

**Sleven's authorisation, from `NEXT.md`:** he authorised the front-page swap on
2026-09-08 and the shape is his — the new page in place of the old one, both
still viewable, **nothing deleted.** Testing site only. No publication change.
Going live stays closed and is not raised.

**What Q54 asks for:** the testing site's front door serves the new page, the
old page is still reachable at a stated address, and **both confirmed from
served bytes** — so this item does not close without a deploy and a fetch.

**First step is the survey, because the item says so.** `NEXT.md` records that
`deploy_pages.py:132` carries `ASSEMBLED = {'index.html'}` — the current front
page is assembled by build logic keyed to that name and deliberately not in
`PAGES` — and that `_SHIP_CONTENT_PAGES` in `build_deploy.py` is keyed by output
name as well. **"Build surveys that ground; do not design the swap around a
guess."** So before anything is edited I am reading exactly what is keyed to the
name `index.html`.

**Two things my own Q53 finding says a swap decides mechanically, and I will
report what the ground says about both rather than assume:**

1. **The gate.** `/` serves `cc-locked` with `#cc-gate`; `/next` serves ungated.
   The gate markup lives in `build_deploy.py`, which is the assembly step keyed
   to `index.html` — so whether the gate follows the NAME or follows the PAGE is
   a fact about that code, not a preference, and it decides whether the swapped
   front door is gated. **I am going to read it, not guess it.**
2. **The four addresses.** `/#matrix`, `/#dev`, `/#calendar`, `/#legend` resolve
   today and do not exist on `/next`. Any link already shared stops resolving
   the moment the front door moves.

**If either turns out to need a decision rather than a mechanism, it gets filed
and I stop on that part** — Q54's own DONE-WHEN does not mention either, and I
am not widening the item on my own judgement.

*Code, 2026-09-11 01:25 CDT.*
