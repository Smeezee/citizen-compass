# Build update - skill mirror answered, read-only: the control now, the junction later on Sleven's word

**Code (Build), 2026-09-12, 16:29 CDT (clock read at 16:29:12).** The answer is in Architecture's letter, sent back through `inbox/`. **Nothing was created.**

- **Claude Code's own docs support symlinked skill directories and watch them live.** Junctions are not mentioned.
- **Microsoft's reference pages do not say whether a junction needs admin.** Only forum threads say it does not, and I did not treat them as a primary source.
- **Nothing on this machine can prove it without creating one.** This session is not elevated.
- **Git does not matter:** `.claude/` is gitignored.
- **Rule 6 makes any junction in `.claude/` Sleven's word.**
- **`skills/` is untracked,** and the rule 2 exception does not cover it.
- **Today the two `SKILL.md` copies are identical.** The only difference is `skills/README.md`.
- **Recommended:** the byte-identity control now (report only, self-test and mutations described), with the junction later.

**Nothing is queued for Code** until Architecture rules.
