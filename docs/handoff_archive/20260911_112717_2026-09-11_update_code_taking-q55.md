# Update — Code is taking Q55

**Received:** "go" from Sleven, 2026-09-11 11:26 CDT / 16:26 UTC. Q54 closed at
03:12 and is confirmed on the served site, so the queue's next item is **Q55 —
the missing features, one at a time.**

**Mail read first (rule 24):** nothing new in `correspondence/open/build/` since
C1's 02:41 freeze letter, which I have read and acted on. Noted in passing from
the owner tray: Sleven ruled the committed repository is the official record and
asked C1 for the narrowest standing commit permission. **That is C1's to answer
and hard rule 2 still binds me, so tonight's work stays uncommitted until he
says otherwise.**

**Q55's DONE-WHEN is not implementation.** It reads: *"each item the inventory
marked ABSENT or CHANGED has its own queue entry, its own DONE-WHEN, and lands
on its own"* — and *"One at a time is the instruction, not a suggestion - a
batch of them is the rebuild he refused, wearing a different name."*

**So the deliverable is the entries, and the entries are not mine to file.**
`NEXT.md` says one writer, C1, and Code never edits it. So I am writing the
proposed entries in full — one per item, each with its own DONE-WHEN — and
routing them to Architecture, which is the desk that writes that file. Rule 26:
route it, do not ask permission to route it.

**What I am NOT doing:** deciding which absent features come back. Q53 records
that as Sleven's, in his words — present, absent, changed, then he decides —
and nothing is carried over or dropped on a desk's judgement. The entries will
say which are already settled, which are mechanically forced by the swap, and
which are waiting on him, and they will not pre-empt the third group.

*Code, 2026-09-11 11:26 CDT.*
