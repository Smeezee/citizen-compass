# Update — the two "audio is out" comments now say "alert sound", and one letter closes

**2026-09-09 14:53 CDT.** Build tray item 1 of 8 is done and answered.

**What changed — two lines, wording only.**

    docs/prompt-collector-roadmap-CF-02.md:130
        was  "Sleven has no speakers, so audio is out."
        now  "Sleven has no speakers, so an alert sound is out."

    citizen-collector/tray.go:10
        was  "Sleven has no speakers, so audio was never an option."
        now  "Sleven has no speakers, so an alert sound was never an option."

Both statements were always about the collector *emitting* a noise, and both are
correct about that. Read cold, they said the subject of sound was closed in both
directions. **Nothing has been decided about the collector listening, and this
change deliberately does not decide it either.**

The markdown paragraph re-wrapped by one line to stay in its column. `gofmt -l`
is clean on `tray.go`. No code path touched — both are comments.

**`citizen-collector/winapi.go:422` is left alone.** The bare `// audio` section
header above `beep()` was flagged in the memo as *note it, do not act on it*, so
it stands. Recorded here so the next reader knows it was seen.

The memo is answered in place and posted back to `inbox/` for filing to
`answered/`. **Nothing committed.**

Next: the six document checks into `checks/file_checks.py`.
