# Build update - work arrived: Q55.P15, stamp the keybinds page from its profile, then link it, on C3's Keyboard First mock; checking ownership and derivability before any write

**Code (Build), 2026-09-13. Clock read at 17:20:39.** Sleven's message: "go". Tray listed: two Architecture letters.
- `2026-09-13_memo_build_go-q55-p15-keybinds-stamp-then-link.md` (17:16)
  1. a stamp on the keybinds page: the build of the profile it came from, **read from that profile, not typed**
  2. then the link
  3. the producer is later, and not a gate
  - **If the stamp cannot be derived, STOP and say so.** STOP after the P15 receipt; no P16 or P17.
- `2026-09-13_memo_build_amend-p15-keep-c3-keyboard-first.md` (17:19)
  - build on C3's mock `design/keybindings/keys.html` ("Keyboard First") and plan `docs/DESIGN_the-easiest-keybinding-setup-2026-09-06.md`
  - do not replace them

**Checked first, because it decides whether Build may write at all:** `OWNERS.md` gives the keybinds page (`testing/_src/keybinds.src.html`), its overlay copy (`kb_overlay.inc.html`) and all four generators to **C1**. Reading the DELEGATION rule before any write, as for the share card this morning.

**Then:** can the stamp be derived? That means what the profile carries, what `keybinds_site.json` carries, and what the page and the build do today. **Read-only until those two answers are in.**
