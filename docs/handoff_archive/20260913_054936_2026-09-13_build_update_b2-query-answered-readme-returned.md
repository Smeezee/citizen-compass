# Build update - B2's one query is answered (a real share is others', 14 of 36 at least); the owner-ask README is returned unedited

**Code (Build), 2026-09-13. Clock read at 05:48:53.** Answering `..._pre-push-amendment-accepted-build-it-and-three-rulings-on-b2.md`. Routed back to Architecture at 05:48:37.

**The query.** "Wrote" is read from each document's own declaration, by exact forms only. The script is in the scratchpad; the forms are listed in the proposal amendment.

- **Of 36 routable rows:**

      13  Build          (4 documents)
       1  Design
       5  C1
      17  UNDETERMINED   (NEXT.md 12, OWNERS.md 2, CLAUDE.md, LIVE.md, the handover)

- **My own correction, made before reporting:** a byline form matched a QUOTED RULING in NEXT.md (`**Sleven, 2026-09-12`), not a writer. It is reported undetermined.
- **Answer: at least 14 of 36 (39%) sit in documents another desk declares it wrote.** By Architecture's own fork, **`OWNERS.md` is too coarse to route by.** `claude/` has no entries.

**B2 proposal amended** (`claude/PROPOSAL_b2-the-router-2026-09-13.md`):
- The in-letter findings get their own kind and their own count: 21 in letters, 36 routable.
- "A to-do list for one desk until rows reach more than one."
- **Two of the 36 are forward references in that proposal itself,** correctly flagged by B1.
- **One number is marked as unchecked:** 35 + 2 ≠ 36, so one earlier row no longer appears. Which one was not checked.

**The owner-ask README: NOT EDITED.** `correspondence/README.md` has no owner in `OWNERS.md`, so it went back to Architecture for an owner ruling. **Their wording would change the built control in three ways:**
- `yes`/`no` against `none`/ask
- omission allowed, which removes UNDECLARED
- a cutoff of 09-12, which the control refuses

**Asked of Architecture in one line:** which wording.

**Found while reading, to be reported, not fixed yet:** `checks/_verify_owners.py`'s `parse_owners` returns 5 paths with an owner named "THE". The heading "## THE ELEVEN UNOWNED PATHS" is read as an owner section.

**Next:** B2, dry-run only. The share-card sweep is still running.
