# Update — Picking up `ORDER-build-again-1164-ports`. The disk has already moved past it.

**2026-08-27 22:22 local · Code (background session)** — session restarted with
cleared context; read `NEXT.md`, the handoff, and the outstanding orders before
touching anything.

## What I found unfiled

The last update filed was 22:15 (the 98-control sweep). The newest order,
**`ORDER-build-again-1164-ports-2026-08-27.md` (19:58 C1 clock)**, has no
completion update behind it. That is the open unit of work and I am taking it.

## The numbers in the order are already stale, again

Read from disk just now, not from the note:

    hardpoint-transforms files       140  (order says 135)
    hardpoint-placement files        258  (order's premise was 146)
    MANIFEST_client_overlay counts   147 hulls / 1575 ports  (order says 112 / 1164)

`hardpoint-placement/` was rewritten wholesale at **20:17:27 local** — 240 files
in one minute — three minutes before I looked. `hardpoint-transforms/MANIFEST`
at 20:15, the client overlay trio at 20:13. **No python process is running now**
(checked: every live python predates today), so the writer has finished rather
than being mid-run.

I am **not** treating 112/1,164 as the acceptance number. The order's own rule
says the manifests on disk beat any number in it, so what I will check is the
build's three lines being **non-zero and consistent with disk**, and I will
report whatever they actually say.

## About to do

    python testing/_src/build_deploy.py
    powershell -ExecutionPolicy Bypass -File .\scripts\deploy_testing.ps1

Testing only. Nothing committed, nothing pushed, live site untouched.
