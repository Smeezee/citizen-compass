# Update — FOUR taken, the two superseded letters answered, my archive corrected, and the design document is not on disk

Filed at the time in this file's archive name.

## DONE, ALL THREE THINGS THE RULING ASKED FOR

**Answered as superseded, naming
`2026-09-10_memo_ruling-four-and-it-freezes-the-other-two-are-superseded.md`:**

    automation-first-...       round three, and its FORWARDED behaviour - dead
    the-full-spec-...          "eight touches" - dead ON THE NUMBER ONLY

**Each answer records what is still live in that letter rather than letting it go
quiet with the dead part.** The full spec's test section - C5, the throwaway
letter, the eight pass conditions, `git status` either side, the usage and which
allowance - is what I am working to and the answer says so.

**My archive entry is corrected and says it was corrected.**
`20260909_234908_update-the-headless-harness...` now carries a marked banner: the
number is FOUR, the behaviour is FREEZE, and the substance of the paragraph is
unaffected because none of the brakes were built.

**`_updates_log.md` carries the original wording twice and I did NOT edit it.** The
Go watcher appends to that file. A second writer on it is the exact failure this
project has already paid for (rule 14), and doing it to fix a wording would be a
worse defect than the wording. **The correction is in the archive entry and in this
update; the log copy is stale and I am saying so rather than leaving it to be
found.**

## `punches - stamps` IS 0 OR 1. TAKEN.

The watcher punches on arrival, the desk stamps on the way out, so a desk holding
the file leaves punches one ahead - correctly. **Nothing was built against the
"always equal" rule.**

**And the daily ceiling is the primary defence, not a backstop** - because a desk
answering with a NEW file starts a fresh card, so four never fires on that path.
Taken with that weight. **The number is not invented and neither is the age rule.**

## THE DESIGN DOCUMENT IS NOT ON DISK

    claude/DESIGN_the-doorbell-the-job-number-and-the-punch-card-2026-09-10.md

**It does not exist.** Searched by exact name, by `DESIGN_*` across the repository,
and by content for "doorbell". The only hits are three memos in
`correspondence/answered/` that *refer* to it. Architecture has been told to write
it; it has not landed.

**So the card cannot be built yet, and I am not building it against your memos
instead** - that is the whole point of the instruction. **Stalled and reported,
not swapped for something else** (rule 25).

## AND THIS IS THE SECOND TIME TONIGHT

    "Its boot prompt already exists at claude/PROMPT_boot-a-new-c5.md"   it did not
    "build it against claude/DESIGN_the-doorbell-...-2026-09-10.md"      it does not

**Two memos in one night named a file as existing when it was not there**, and both
times the file was central to the job. The first I could resolve by research - C5's
charter is in `docs/CURRENT-STATE.md`, so the boot prompt was composed from it and
marked as composed. **The second I cannot**, because a design is a decision and
there is nothing to derive it from.

**This is the same shape as the `_to_delete/python_handoff_path_retired_20260801/`
correction in `CLAUDE.md`** - a document confidently describing a location that is
not there. It cost nothing tonight because both were caught on the first look.
**It is worth a cheap control:** a memo naming a repository path that does not
exist could be flagged when the watcher files it. Not built, not asked for -
raised because twice in one night is a pattern and the third one may not be caught.

## STILL AT THE LINE

`scripts/wake_desk.py` exists and **nothing calls it.** No counter, no ceiling, no
lock - which your ruling says is fine as long as nothing calls it. Nothing does.

**The go to spend is yours and I have not taken it.**

Nothing committed.
