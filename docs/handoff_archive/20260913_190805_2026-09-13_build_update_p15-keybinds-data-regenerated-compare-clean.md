# Build update - P15 data fix DONE: keybinds_site.json regenerated from the 4.10.0-hotfix profile, and the compare is CLEAN (1,103 of 1,103); stamp and link are now being wired

**Code (Build), 2026-09-13.** Architecture's (b) plus GO (`..._p15-hold-link-go-fix-nested-bindings.md`), under the delegation recorded at 18:56.

**What was built (Code's, uncommitted):**
- **`kb_profile.py` (new; OWNERS line requested):** the ONE reader of the default profile.
  - It reads inputs from the attribute and from child elements (`<gamepad input>`, `<keyboard><inputdata>`), in file order, with exact repeats dropped.
  - Blank forms `""`, `" "` and null mean unbound, and nothing else is folded.
  - It reads all three activation spellings. **Two spellings on one element are refused**, as is a duplicate action.
  - Per-device activation modes are kept per device.
- **`checks/_verify_keybinds_stamp.py` (new):** a self-test of **25 of 25** planted cases, written by hand from every shape measured in CIG's file.
  - **Rule 12: 12 of 12 mutants of `kb_profile.py` caught by a named case.**
  - The first run had 2 survivors, both test weaknesses:
    - no case regenerated a row carrying a stray value
    - the tie-for-newest case passed for the wrong reason, because a missing file refused first
  - Both are fixed, and a crash is never counted as a catch.
- **`extract_default_profile.py --site` (C1's; delegated):** it rebuilds the bindings in `keybinds_site.json`. Row order and every text field are kept. It is a dry run by default, and refuses unless the file round-trips byte for byte.

**The regeneration (`--site --write`):**
- **44 rows changed, 0 dropped, 0 added.**
- **Compare: 0 of 1,103 actions differ.** Before, 44 differed: the 58 child-element inputs, the scoreboard activation, and 20 actions with per-device modes.
- **The extra inputs** (for example Chat Window Focus `enter` plus `np_enter`) sit in a new optional `more_inputs`, and **per-device modes** in `activation_by_device`. Every existing reader still reads the same string fields.
- **The git diff is only the changed rows:** 201 lines added, 76 removed.
- **Regenerated by their own generators** (`build_kb_actions.py`, delegated; `build_keybind_modes.py`, run and not edited): `kb_actions.gen.js` (1 line) and `kb_modes.gen.js` (3 lines).

**The derived stamp, as the module prints it:** "Default bindings verified identical to Star Citizen 4.10.0-hotfix (change 12545750, built Aug 28 2026) - checked 1103 of 1103 actions at build."

**Now:**
1. the marker in `keybinds.src.html` and the link in `next.src.html` (both delegated; written)
2. the `build_deploy.py` fill and the `_verify_deploy_drift.py` declaration (Code's; written)
3. build, verify, and the small-screen spot-check

**One display limit, stated rather than fixed:** the page's `defaultBind` shows one input per device, so Chat Window Focus shows `enter` and not `np_enter`. The data carries both; showing it is a page-code change that was not delegated.
