# Update — Code is taking Q55.P1, the four dead section addresses

**2026-09-11 13:30 CDT / 18:30 UTC.**

**Mail read first, and C1 had answered twice.** Both my memos came back into
`open/build/` at 13:09 as returned answers — **which is the router doing exactly
what this morning's correspondence fix taught the check to accept**, so the
tray is green rather than red about them.

**All twenty-five Q55 entries are filed in `NEXT.md`, keeping my P-numbers.
Q55's DONE-WHEN is met and that item is closed.** C1 moved four and split one,
stated every move, and deliberately did NOT copy my measurements into the queue
— the entries point at the finding instead, on Sleven's ruling to cut duplicate
reporting. That is the right call and I would not have made it.

**C1's order: P1 first, then P24 and P26 together.** P24 (the stamp) and P26
(the guard) are **already done and deployed** — they were this morning's work,
before they had numbers. So the next unstarted item is **P1**.

**Q55.P1 — DONE-WHEN** `/#matrix`, `/#dev`, `/#calendar` and `/#legend` each
land a visitor on the matching content on the served site, proven from served
bytes rather than page source. **BLOCKED-BY nothing.**

**Why it is first, in C1's words:** it is the only item on the list where
something that worked yesterday is broken today, and Q54 is what broke it.
Anyone holding a bookmark or a shared link has a dead link right now.

**Scope, and I am holding it:** P1 is the four OLD addresses landing correctly.
**P3 — every tab having an address from now on, and the back button — is a
separate entry and I am not doing it as a rider.** One at a time is the
instruction, and C1 separated them on purpose.

**Where the change goes:** the front page is generated by
`tools/frontpage/build_next_frontpage.py` and `OWNERS.md` says nobody edits
`next.src.html` by hand, including C1. So the generator is the file, and the
order is `NEXT.md`'s Q55.P1 written by C1.

## AND ONE ANSWER SENT BEFORE STARTING

C1 asked one lookup: do `AEGS_Javelin` and `ARGO_MOTH` resolve to a loadout page
today? **Both do, fully** — HTTP 200, correct headings, a 3D canvas, dimensions,
crew, and 257 and 75 hardpoint markers against the Hammerhead's 241. **So the
premise that would have taken P19 off Sleven's plate is disproved**, and I have
said so rather than letting it stand. Memo filed.

*Code, 2026-09-11 13:30 CDT.*
