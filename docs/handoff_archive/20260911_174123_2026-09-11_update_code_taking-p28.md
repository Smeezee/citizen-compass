# Update — Code is taking Q55.P28, the refused join that sends the visitor away

**2026-09-11 17:41 CDT / 22:41 UTC.** Mail read first: my P27/P20 memo is in
Architecture's tray unanswered; nothing new addressed to me.

**Q55.P28 — DONE-WHEN** a row whose ship-page join is refused shows that we
could not identify its page, rather than linking outward as though RSI were the
right destination — **or** a dated decision records that outward is intended and
why. **BLOCKED-BY nothing.**

**The refusal itself is correct and stays.** `build_frontpage_data.py` refuses
the Javelin (two candidate paths of equal evidence) and the MOTH (no published
dimensions). **That is rule 19 working**, and P28 is about the CONSEQUENCE: the
refusal is about our confidence in a NAME JOIN, and the outward link states a
fact about the SHIP.

**C1 names the one fact that settles the larger question and says I have it:**
*"are the Javelin's two candidate paths resolved by whatever the ship page
renders?"* **The ship page serves `AEGS_Javelin` with 257 markers while the front
page refuses to name its page at all — two parts of one system holding different
answers to "do we know which page this ship has."**

**So this starts as a trace, not an edit**, and the thing I am looking for is
whether the ship page RESOLVES the ambiguity or merely PICKS. **That distinction
decides the whole item:**

- **if the ship page resolves it on evidence**, the front page can adopt that
  answer with the reason recorded, and the refusal narrows to the MOTH
- **if the ship page just takes the first candidate**, then adopting it would be
  rule 19 violated one layer down — an ambiguity resolved by picking — and the
  honest fix is the front page SAYING it could not identify the page, while
  somebody decides whether the ship page should be refusing too

**I am not going to assume the first.** A resolution that looks principled
because it produces one answer is the thing this project keeps finding.

**Also recorded from my own sizing, and C1 has already filed it:** the MOTH half
and P20's 35th row are one root cause. **Fix the MOTH's hull record and both
halves move** — but that is a data fix in another desk's file and it is not what
P28's DONE-WHEN asks for.

*Code, 2026-09-11 17:41 CDT.*
