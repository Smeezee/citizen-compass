# Update — Code is taking Q55.P3, giving every tab an address

**2026-09-11 15:44 CDT / 20:44 UTC.** Mail read first: nothing new since
Owner's 13:55 letter, which is closed.

**Q55.P3 — DONE-WHEN** a visitor on any tab can copy the URL, send it, and the
recipient arrives on the same tab; and the back button returns to the previous
tab. **Proven on the served site. BLOCKED-BY nothing.**

**Why it is next:** P1 and P2 are closed, P24 and P26 are deployed, P18 turned
out not to be a defect, and P4 is blocked by a Group B decision that is
Sleven's. **P3 and P20 are what is left unblocked in Group A, and P3 is the
other half of the mechanism I built for P1** — so it is the one that belongs
next while that code is still in hand.

**What P1 deliberately left undone, in its own comment:** *"This opens the page
on the right tab. It deliberately does NOT write the hash back when a tab is
clicked, and does not handle the back button — that is Q55.P3."* **That is the
work.**

**The shape I intend, stated before I write it so the choice is on the record:**

- **a tab click writes the page's OWN name for the tab** (`#ships`, `#dev`,
  `#cal`, `#legend`), not the old page's. New links get the current
  vocabulary.
- **the old names keep working.** `#matrix`, `#calendar` and the rest stay in
  the map, because P1 exists so that nobody's bookmark gets rewritten, and a
  visitor arriving on `/#matrix` is NOT redirected to `#ships` — silently
  changing somebody's URL is its own small defect.
- **the back button comes from the history entry the hash creates**, with one
  handler applying the hash however it changed — landing, clicking, back, or
  forward. One path, not four.

**Scope:** `?q=` sharing a search stays out. Q53 measured it ignored on BOTH
pages, so it is not a regression and it is not in P3's DONE-WHEN.

*Code, 2026-09-11 15:44 CDT.*
