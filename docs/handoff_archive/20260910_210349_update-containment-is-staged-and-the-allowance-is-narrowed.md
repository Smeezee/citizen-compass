    state        BLOCKED
    blocked_on   the switch, and his ruling on Echo's Option 1
    clears_by    OWNER

Filed at 21:04 CDT / 02:04 UTC — `date` run at 21:01:59, this typed after it.

# CONTAINMENT IS STAGED. THE ALLOWANCE IS NARROWED, CONTROLLED AND PROVED. NO
# WAKE HAS RUN.

**Report in his tray:
`2026-09-11_memo_owner_containment-is-staged-and-the-only-thing-left-is-your-switch.md`.**

## WHAT "GO" WAS TAKEN TO MEAN, AND WHAT IT WAS NOT

**Item 1 of the step B request — narrow the allowance, build the control. Mine,
costs nothing, wakes nothing.**

**Not item 2, not item 3.** The probe needs the switch; the switch does not
exist; creating it is his act. **The gate refused three times this afternoon and
would refuse again tonight, and I did not go near it.**

Staging is also the first line of Echo's own recommended order, so it is the
right work whichever way the word was meant.

## THE CHANGE

    was    an Edit allowance over the WHOLE of inbox/
    now    Read,Glob,Grep,Edit(inbox/_replies/**)

**Named once and derived everywhere** — `REPLY_DIR_NAME` → `REPLY_DIR_REL` →
`WRITE_ALLOWANCE`. There were four copies of the old literal; there are now none.

**The probe gained a third attempt: a write into a PROTECTED folder, which must
be refused.** The first two prove a desk is confined to the repository; the third
proves it is confined inside `inbox/` as well — the half that was never tested.
**If `protected_folders.txt` cannot be read, that attempt is NOT PERFORMED and
the verdict says the pass covers the repository boundary only.**

## THE CONTROL, AND THE ASSERTION I WOULD KEEP IF I COULD KEEP ONE

`checks/_verify_reply_path.py`, 11 assertions, `RULE16: INDEPENDENT` — protected
names from the watcher's file, the allowance from the launcher's source, neither
imported.

**The collision everyone expects is the allowance reaching a protected folder.
The one that will actually bite is the reverse:** add `_replies` to
`protected_folders.txt` and the watcher stops reading the only directory a woken
desk may write to. **Every reply silently never filed. No error anywhere.**

## PROVEN BY BREAKING IT FOUR WAYS

    UNTOUCHED - the real files                            0 findings   OK
    the reply directory added to protected_folders.txt    2 findings   OK
    protected_folders.txt naming nothing                  1 finding    OK
    the launcher's constant deleted                       1 finding    OK
    a wide Edit allowance left in the launcher's code     1 finding    OK

**The third is the one I would have missed.** An empty protected list makes every
path assertion vacuously true and the control goes green having tested nothing.
It fails instead, and says so.

## AND A DEFECT IN MY OWN CONTROL, ON ITS FIRST RUN

    PASS  and it names at least one folder  - it named none, so nothing below
                                              would have been tested

**A PASS with a detail line saying the opposite of the truth**, because the
detail printed unconditionally. Nobody would have been misled tonight — the
verdict was right — **and that is precisely how a reassuring line survives.**
Fixed on sight.

## RE-RUN AFTER THE CHANGE

    _verify_api_key_guard.py   36/0        _prove_launcher_gate.py   4 planted, correct
    _prove_switch.py           32/0, 2 NP  _prove_wake_record.py     holds
    _verify_reply_path.py      11/0        _prove_reply_path.py      5 states, correct
    _verify_rule16_labels.py   GREEN       wake_desk.py --self-test  exit 0

132 controls on disk, up one.

## STILL HIS

**The probe wake, and the switch that gates it.** Everything staged, per Echo's
Option 1 line one. Nothing else started: no brakes, no doorbell, no second wake,
timezone untouched, `memo.go` unopened since the swap, nothing committed.

**His three other letters answered in the memo:** the export folder location was
my error and my own audit is the evidence against it; the date-prefix
investigation is withdrawn and I have not done it; the two-dates check is mine
and sits behind containment and the brakes.
