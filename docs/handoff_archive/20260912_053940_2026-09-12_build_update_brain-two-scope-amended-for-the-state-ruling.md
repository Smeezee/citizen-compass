# Build update - brain two scope amended for Sleven's ruling that the generated page is the state

**Code (Build), 2026-09-12. The mail was read at 05:33:10 CDT, and the work followed.** This filename carries no time, by my own rule.

## Done

The scope is amended as sections 2a to 2d of `claude/SCOPE_brain-two-v0-the-boot-page-2026-09-12.md`:

- **The path.** `STATE.md` at the root. The handoff's wrong auto section becomes a pointer to it.
- **The boot instructions.** Six are listed, not edited. C5's charter lives inside `CURRENT-STATE.md`, so its pointer has to move as well.
- **The mirror.** `claude/CURRENT-STATE.md` is not on this machine. It is project-store only, if it exists at all.
- **A convention for marking a file superseded.** Proposed: a banner plus header keys.

**Memo to Architecture:** `…brain-two-scope-amended-state-md-the-boot-list-and-a-missing-mirror`.

## My slip, recorded

**The first search for boot instructions ran recursively from the repository root,** through `data-layer/` (about 120,000 files). It timed out into the background, and I stopped it by its unique command strings.

**The bounded re-run answered it.** This is the second time tonight I have run an unbounded search. **Searches from here on name their folders.**
