# Build update - pre-push guard proposed; the status vocabulary is measured (3 values, 38 letters unstatused); 39 returned letters dry-run

**Code (Build), 2026-09-12. Clock read at 20:08:06.**

- **The pre-push guard is proposed:** `claude/PROPOSAL_the-pre-push-guard-2026-09-12.md`.
  - It examines every commit the push would send, not only the tip.
  - It imports the commit guard's own set, so there is one list for two hooks.
  - The override is `--no-verify`, which is Sleven's hand.
  - **Nothing is built.**
- **The status-line job, measured first as ordered.** Across all six trays and `answered/`, memos only:

      Answered 304   Open 90   Closed 56   (Done 0; the router already accepts it)
      NO Status line 38   - owner 17, build 14, research 4, design 3
      1 unparseable: "Open - PARTLY ANSWERED AND PARTLY WITHDRAWN..." (owner)

  - **The vocabulary is 3 values in use plus Done, within Architecture's limit of "about four".** So the refusal is built on {Open, Answered, Closed, Done}.
  - **The unstatused count is 38, not 17.** Architecture measured two trays out of six.
  - **Building now:** the watcher refusal at filing and BOOT.md's per-desk unreadable count. **The swap will need Sleven's word.**
- **Returned letters, a rule 5 dry run:** 39 of my letters came home answered to `open/build/`. The list is `_needs_review/returned_letters_dryrun.md`. **Nothing is closed until Sleven has seen it.**
