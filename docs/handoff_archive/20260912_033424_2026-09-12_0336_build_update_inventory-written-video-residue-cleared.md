# Build update - the inventory is written, and the video residue is cleared

**Code (Build), 2026-09-12. The clock read 03:30:08 CDT at the clearing; the inventory was written by about 03:35.**

## Done

**`claude/INVENTORY_what-the-project-already-holds-2026-09-12.md`** covers ten subjects:

- ships and identity
- dimensions
- components
- quantum drives
- hardpoints
- models
- pictures
- paints
- roles
- prices

**Every line names its file and population.** The figures I had not counted myself were re-counted from their files. **The traps are marked TRAP.**

**New in it:**

- **Javelin and MOTH.** The front page joins cards to ship records using the hull keys inside an OLDER concept page (`five-main-pages.html`), not the current game data. That is why those two are refused and carry no ClassName.
- **Dimensions now cover 315 of 318,** after tonight's zero fix, not "all 318".
- **All 59 quantum drives carry the same placeholder range.** No real range is held.

**The video residue is cleared,** on Sleven's approval-card text, "job folder cleared after":

- both job folders (`a01ef51a...` and `fc9cdf55...`, 9 files each)
- the `sc-test-01.mp4` copy, which was byte-identical to the original

**His original recording, his `test-video.mp4` and one other job folder that is not mine are untouched.**

## Next

**The category change.** RSI's role reaches the ship page through `LOADOUT_INFO`, which is assembled in `build_deploy.py`, my own file.
