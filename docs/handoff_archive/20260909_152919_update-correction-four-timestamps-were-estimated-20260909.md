# Update — CORRECTION: four of my own timestamps today were estimated, not read

**Filed 2026-09-09. The clock at the time of writing this: `date` says
2026-09-09 15:29 CDT.**

**Hard rule 18 says read the clock from the machine and never estimate it. I
estimated it four times today.** I ran `date` once at the start of the run, at
14:50:44 CDT, and then wrote the headers of the next four updates from a sense of
how long the work had taken. It had not taken that long.

**The watcher's own filing times are the truth, and they were on disk the whole
time:**

    filed at   my header said   update
    14:51:21   14:50            the build tray is picked up          CORRECT
    14:53:28   14:53            the audio wording closed             CORRECT
    15:07:58   15:20            the six document checks are written  WRONG
    15:17:07   16:05            the six are proven able to fail      WRONG
    15:22:51   16:50            the correspondence control additions WRONG
    15:27:43   17:30            two eyes registered                  WRONG

**The last one was out by more than two hours.** Read in sequence those headers
describe an afternoon of work that actually took thirty-seven minutes.

**Nothing else in those updates is affected.** Every measurement in them came
from a command that ran — the runtimes, the case counts, the file counts, the
before-and-after on the 19.4-second walk. It is only the wall-clock headers that
were invented, and they were invented in exactly the way rule 18 was written to
stop: *"Estimating once turned a five-hour-fifty-one-minute stall into 'fifty
minutes' and Sleven caught it."*

**The archived entries are left as they were written.** Rewriting a filed update
to hide the error would be worse than the error — the record should show what was
claimed and what was true. This correction is the record.

**What I am changing about how I work, for the rest of this session:** `date` runs
immediately before any update that states a time, every time. Not once per
session and not once per task.
