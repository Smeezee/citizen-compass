# Update - B3 IS LIVE ON TESTING. The front page went 2.37 MB -> 0.12 MB and the pictures are cacheable files.

    https://citizencompasstesting.citizencompass-contact.workers.dev/next
    Version ID 12d786ea-c95c-4aef-9609-9d5a9c4a05de

## Sweep

    120 ok, 0 failed, 3 skipped, 0 NOT RUN, in 1822s
    receipt 2026-09-06T16:39:09   fingerprint e9520adb4df9330a   exit 0

No `-IgnoreSweep`. The gate confirmed the receipt against these exact bytes.

## What went up

    242 files: 241 images + next.html   (524 already uploaded)
    payload now 766 files, 519.7 MB

## Verified from SERVED BYTES

    /next                    200, 120,898 bytes
    served sha256            8fc457226b8dc881
    local  sha256            8fc457226b8dc881    IDENTICAL

    /images/00951fabe22ff106.webp   200, 6,544 bytes, image/webp
    served sha256            00951fabe22ff106
    local  sha256            00951fabe22ff106    IDENTICAL

**The image's served hash IS its filename.** C1's naming scheme makes every
picture self-verifying on the wire - you cannot serve the wrong bytes under a
correct name, because the name is the bytes.

    data: URIs left in the served page   0
    images/ references in served page  246
    cc-src-note                          4   source + takedown notice, served
    not affiliated                       1
    /  (the OLD front page)            396,153 bytes, unchanged

## The result

    next.html   2,496,384 -> 120,898 bytes on the wire   -95.2%
    241 pictures now cacheable files instead of base64 inlined into the HTML

The 0.12 MB is what blocks first paint. Before today it was 2.4 MB.

Rendering was confirmed against the built payload before the deploy - 253 cards,
one height, 246 of 246 pictures, 18 groups A-Z, ships A-Z inside, no page errors -
and the served page is byte-identical to what was tested.

## Open, and none of it moved by this deploy

- **241 unreferenced images, 3.96 MB, still published.** Rule 5 report is filed
  and I have moved nothing. Waiting on Sleven.
- **B2** blocked on his edition ruling. **B1** behind B2.
- **`build_frontpage_data.py`** untracked; committing needs his go-ahead (rule 2).
- Nothing committed, nothing pushed, no `git add -A`.
