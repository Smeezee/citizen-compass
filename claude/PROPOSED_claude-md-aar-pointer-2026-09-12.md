# PROPOSED patch — CLAUDE.md pointer for AAR / LESSONS (not a numbered hard rule)

Insert **after** rule 27 (Owner-ask gate) and **before** `## Standing rule: keep LATEST_HANDOFF.md current`.

C1: check wording against PROCEDURE; apply if accepted.

---

```markdown
## After-action lessons (not a hard rule)

**Skills are not this file.** Reusable skills live in `skills/` (mirrored for Claude Code under `.claude/skills/`). Wiping or rewriting `CLAUDE.md` must not delete the skill pack.

**Project lessons** live in `claude/LESSONS.md`. Read that file **before similar work**, not at every boot (`BOOT.md` remains the boot page).

**AAR procedure:** `claude/PROCEDURE_the-after-action-report-2026-09-12.md`.
**AAR skill:** `skills/aar-loop/SKILL.md`.
AARs **propose** rule or skill edits; they never apply them silently. Rule 27 still applies to any Owner ask that comes out of an AAR.
```

---

End of proposed patch.
